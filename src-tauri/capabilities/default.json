{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the Space Power main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-close"
  ]
}
